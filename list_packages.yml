- name: List_Packages
  hosts: all
  tasks:
    - name: yum_command 
      yum:
        list=installed
      register: yum_packages

    - debug: var=item
      with_items: "{{yum_packages|json_query(jsonquery)}}"
      vars:
        jsonquery: "results[?name=='tar']"
